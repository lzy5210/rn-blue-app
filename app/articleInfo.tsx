
import { Text, SafeAreaView, StyleSheet, View, Alert, Keyboard, TouchableOpacity } from 'react-native'

export default function ArticleInfoScreen() {
    return (
        <Text>
            ArticleInfoScreen
        </Text>
    )
}